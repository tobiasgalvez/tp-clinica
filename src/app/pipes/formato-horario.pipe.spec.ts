import { FormatoHorarioPipe } from './formato-horario.pipe';

describe('FormatoHorarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoHorarioPipe();
    expect(pipe).toBeTruthy();
  });
});
