import { FechaEspanolPipe } from './fecha-espanol.pipe';

describe('FechaEspanolPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaEspanolPipe();
    expect(pipe).toBeTruthy();
  });
});
