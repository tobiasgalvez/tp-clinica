import { RotateOnHoverDirective } from './rotate-on-hover.directive';

describe('RotateOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
