import { HoverColorHeaderDirective } from './hover-color-header.directive';

describe('HoverColorHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverColorHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
