import { HoverColorCerrarSesionDirective } from './hover-color-cerrar-sesion.directive';

describe('HoverColorCerrarSesionDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverColorCerrarSesionDirective();
    expect(directive).toBeTruthy();
  });
});
